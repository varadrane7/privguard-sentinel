{
  "name": "privguard-sentinel",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["scanner"],
  "scripts": {
    "build": "npm run build --workspace=scanner",
    "scan": "npm run build --workspace=scanner && node scanner/dist/index.js"
  }
}
