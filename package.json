{
  "name": "privguard-nexus",
  "version": "2.0.0",
  "private": true,
  "workspaces": ["scanner", "dashboard"],
  "scripts": {
    "scan": "npm run build --workspace=scanner && node scanner/dist/index.js",
    "dev": "npm run dev --workspace=dashboard",
    "dev:dashboard": "npm run dev --workspace=dashboard"
  }
}
